@model IEnumerable<FisioterapiaUlatskawa.Models.AntecedentesViewModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.nombreAntecedente)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.cedula)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nombreAntecedente)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.cedula)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idAntecedente }) |
            @Html.ActionLink("Details", "Details", new { id=item.idAntecedente }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idAntecedente })
        </td>
    </tr>
}

</table>
